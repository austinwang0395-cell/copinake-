const menu=document.querySelector('.menu'),nav=document.querySelector('.nav-wrap nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('form')?.addEventListener('submit',e=>{e.preventDefault();const b=e.currentTarget.querySelector('button');b.textContent='Thank you — we will be in touch.';b.disabled=true});if(window.COPINA_PRODUCTS){const all='All equipment',filters=document.querySelector('#catalog-filters'),grid=document.querySelector('#catalog-grid'),categories=[...new Set(COPINA_PRODUCTS.map(p=>p[0]))];let active=all;function render(){filters.innerHTML=[all,...categories].map(x=>`<button class="${x===active?'selected':''}" data-filter="${x}">${x}</button>`).join('');grid.innerHTML=COPINA_PRODUCTS.filter(x=>active===all||x[0]===active).map(([category,name])=>`<article class="catalog-card"><div class="catalog-image" style="background-image:url('${COPINA_CATEGORY_IMAGES[category]}')"><span>${category}</span></div><div><h3>${name}</h3><p>Commercial kitchen equipment</p><a href="index.html#enquiry">Request specification <span>→</span></a></div></article>`).join('');filters.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>{active=b.dataset.filter;render()}))}render()}
